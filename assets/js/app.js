/**
 * Vue.js App Configuration
 *
 * @package CBD_AI_Theme
 * @since 1.0.0
 */

// This file will be used when Vue components are loaded via Vite
// For now, components are loaded directly in templates

// Only log if Vue is available
if (typeof Vue !== 'undefined') {
	console.log('CBD AI Theme - Vue.js app loaded');
}

